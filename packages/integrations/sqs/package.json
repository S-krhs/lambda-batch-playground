{
	"name": "@eskra-aws-playground/integration-sqs",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"exports": {
		"./sqs-message-sender.js": {
			"types": "./dist/sqs-message-sender.d.ts",
			"default": "./dist/sqs-message-sender.js"
		}
	},
	"dependencies": {
		"@aws-sdk/client-sqs": "^3.1079.0"
	},
	"scripts": {
		"clean": "rm -rf dist",
		"build": "npm run clean && tsc -p tsconfig.json",
		"test": "vitest run",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"lint": "biome ci .",
		"validate": "npm run typecheck && npm run lint && npm run test"
	}
}
