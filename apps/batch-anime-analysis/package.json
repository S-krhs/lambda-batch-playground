{
	"name": "@lambda-batch-playground/batch-anime-analysis",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"clean": "rm -rf dist",
		"build": "npm run clean && tsc -p tsconfig.json",
		"local": "tsx src/local-runner.ts",
		"test": "vitest run",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"lint": "biome ci .",
		"validate": "npm run typecheck && npm run lint && npm run test"
	},
	"dependencies": {
		"@aws-sdk/client-sqs": "^3.1079.0",
		"@lambda-batch-playground/integration-discord": "0.1.0",
		"@lambda-batch-playground/libs": "0.1.0",
		"@lambda-batch-playground/libs-browser": "0.1.0",
		"@lambda-batch-playground/repositories": "0.1.0",
		"cheerio": "^1.1.2",
		"dotenv": "^17.4.2"
	},
	"devDependencies": {
		"tsx": "^4.20.0"
	}
}
