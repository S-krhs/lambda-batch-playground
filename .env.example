# ローカル用 Neon branch(local)の接続文字列を入れる。develop 用の値は置かない。
# DATABASE_URL: pooled 接続(runtime 用。ローカルは direct と同じ値でもよい)
# DIRECT_DATABASE_URL: direct 接続(prisma migrate 用)
DATABASE_URL=
DIRECT_DATABASE_URL=
